# Samantha AI Guestbook Implementation Tasks

- [/] 1. Create `guestbook.html` with UI layout (Glassmorphism design).
- [ ] 2. Integrate Firebase Auth (Google) and Firestore (Comments) scripts into `guestbook.html`.
- [ ] 3. Update global navigation (`gnb-nav`) in all HTML pages to include `GUESTBOOK` link.
- [ ] 4. Instruct user on Firebase Console setup (Auth, Firestore Rules, Web App SDK keys).
